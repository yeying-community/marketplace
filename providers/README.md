# Model Providers

这里用于维护 Chat 可发现、可配置的模型服务商说明和能力声明。

当前阶段模型服务商主要由 Chat 和 Router 返回的模型元数据驱动，本目录先作为市场扩展入口保留。后续每个服务商包应说明：

- 服务商名称和入口
- 支持的模型类型
- 支持的端点，例如 `/v1/responses`、`/v1/messages`
- 是否支持多模态、工具调用、结构化输出
- 计费方式和购买入口
- 配置字段和鉴权方式

模型服务商包只描述接入能力，不应该包含用户 API Key。
